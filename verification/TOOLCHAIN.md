# Toolchain record

Both developments pin:

```text
Lean 4.19.0
Mathlib v4.19.0
```

During the author's successful compact setup, Lake checked out Mathlib at:

```text
c44e0c8ee63ca166450922a373c7409c5d26b00b
```

The `lean-toolchain` and `lakefile.lean` files are committed in each project.
For the strongest reproducibility record, also commit the `lake-manifest.json`
generated by each successful `lake update` invocation. The manifests must come
from the successful local builds; do not hand-edit or invent them.
